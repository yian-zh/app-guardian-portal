// Local placeholder data for the Notifications page, standing in for
// the Laravel API response until real notifications are generated.
// Each notification is expected to look like:
// { type, title, description, timestamp, tone }
// Empty until the system generates real alerts.
export const notifications = []
